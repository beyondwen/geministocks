import React from 'react';

// Per coding guidelines, API key should be managed via environment variables (process.env.API_KEY), not through a UI.
// This component is therefore disabled to align with security and configuration best practices.
const ApiKeySetup: React.FC = () => {
  return null;
};

export default ApiKeySetup;
